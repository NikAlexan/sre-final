apiVersion: v1
kind: Service
metadata:
  name: prestashop
  namespace: ecommerce
spec:
  type: LoadBalancer
  selector:
    app: prestashop
  ports:
    - name: http
      port: 80
      targetPort: 8080
